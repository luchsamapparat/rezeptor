apiVersion: v1
kind: Service
metadata:
  name: rezeptor
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    - name: oauth2-proxy
      protocol: TCP
      port: 4180
      targetPort: 4180
  selector:
    app: rezeptor
